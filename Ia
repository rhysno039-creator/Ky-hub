-- Memuat WindUI Library Resmi
local Success, WindUI = pcall(function()
    return loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/refs/heads/main/main.lua"))()
end)

if not Success or not WindUI then
    Success, WindUI = pcall(function()
        return loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
    end)
end

if not Success or not WindUI then
    warn("Gagal memuat WindUI Library!")
    return
end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer
local ExecutorName = identifyexecutor and identifyexecutor() or "Executor"
local ThumbnailUrl = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. LocalPlayer.UserId .. "&width=420&height=420&format=png"

-- Window Utama WindUI
local Window = WindUI:CreateWindow({
    Title = "KyZen HUB",
    Icon = "",
    Author = "kyzen868", 
    Folder = "KyZenHubConfig",
    Size = UDim2.fromOffset(580, 460),
    Transparent = true,
    Theme = "Dark",
    SideBarWidth = 160,
    HasOutline = true
})

-- ==================== TAB 1: HOME ====================
local HomeTab = Window:Tab({ Title = "Home", Icon = "home" })
HomeTab:Section({ Title = "1. Welcome Section" })
HomeTab:Image({ Image = ThumbnailUrl })
HomeTab:Paragraph({ Title = "Halo, " .. LocalPlayer.Name .. " 👋", Desc = "• Executor: " .. ExecutorName })

HomeTab:Section({ Title = "2. Main Button" })
HomeTab:Button({
    Title = "Join Saluran WhatsApp",
    Callback = function()
        pcall(function() setclipboard("https://whatsapp.com/channel/0029VbDTYKY9cDDhMKZB3H0J") end)
        WindUI:Notify({ Title = "Berhasil", Content = "Link WA disalin ke clipboard.", Duration = 3 })
    end
})
HomeTab:Button({
    Title = "Akun TT kyzen868",
    Callback = function()
        pcall(function() setclipboard("kyzen868") end)
        WindUI:Notify({ Title = "Berhasil", Content = "Username TikTok disalin.", Duration = 3 })
    end
})

HomeTab:Section({ Title = "3. Update Log" })
HomeTab:Paragraph({ Title = "Riwayat Pembaruan Hub", Desc = "• v11.0: Added Settings Tab for UI Themes & Font customization." })

HomeTab:Section({ Title = "4. Action" })
HomeTab:Button({
    Title = "Destroy / Unload HUB",
    Callback = function() Window:Destroy() end
})


-- ==================== TAB 2: INFORMATION ====================
local InfoTab = Window:Tab({ Title = "Information", Icon = "info" })
InfoTab:Section({ Title = "1. Player Info" })
InfoTab:Paragraph({ Title = "Informasi Akun", Desc = "• Username: " .. LocalPlayer.Name .. "\n• UserId: " .. LocalPlayer.UserId .. "\n• Account Age: " .. LocalPlayer.AccountAge .. " Hari" })
InfoTab:Section({ Title = "2. Game Info" })
InfoTab:Paragraph({ Title = "Status Game", Desc = "• PlaceId: " .. game.PlaceId .. "\n• JobId: " .. game.JobId .. "\n• Player Count: " .. #Players:GetPlayers() })


-- ==================== TAB 3: PLAYER ====================
local flyToggled = false
local flySpeed = 50
local bodyVelocity, bodyGyro
local walkSpeedEnabled = false
local customWalkSpeed = 16
local jumpPowerEnabled = false
local customJumpPower = 50
local noclipEnabled = false
local bunnyHopEnabled = false
local infJumpEnabled = false
local antiAfkEnabled = false

local PlayerTab = Window:Tab({ Title = "Player", Icon = "user" })

LocalPlayer.Idled:Connect(function()
    if antiAfkEnabled then
        local vu = game:GetService("VirtualUser")
        vu:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
        task.wait(1)
        vu:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    end
end)

PlayerTab:Section({ Title = "1. Kontrol Utama" })
PlayerTab:Toggle({
    Title = "Fly", Default = false, 
    Callback = function(V) 
        flyToggled = V
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid")
        if flyToggled and hrp and hum then
            hum.PlatformStand = true
            bodyVelocity = Instance.new("BodyVelocity", hrp)
            bodyVelocity.MaxForce = Vector3.new(9e9, 9e9, 9e9)
            bodyVelocity.Velocity = Vector3.new(0, 0, 0)
            bodyGyro = Instance.new("BodyGyro", hrp)
            bodyGyro.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
            bodyGyro.P = 20000
            bodyGyro.CFrame = Camera.CFrame
        else
            if hum then hum.PlatformStand = false end
            if bodyVelocity then bodyVelocity:Destroy() end
            if bodyGyro then bodyGyro:Destroy() end
        end
    end
})
PlayerTab:Input({
    Title = "Fly Speed", Default = "50", Placeholder = "Masukkan angka...", 
    Callback = function(val) local num = tonumber(val) if num then flySpeed = num end end
})
PlayerTab:Toggle({
    Title = "Walk Speed", Default = false, 
    Callback = function(V) walkSpeedEnabled = V local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") if hum then hum.WalkSpeed = V and customWalkSpeed or 16 end end
})
PlayerTab:Input({
    Title = "Walk Speed Value", Default = "16", Placeholder = "Contoh: 16",
    Callback = function(value) local num = tonumber(value) if num then customWalkSpeed = num end end
})
PlayerTab:Toggle({
    Title = "Jump Power", Default = false,
    Callback = function(state)
        jumpPowerEnabled = state
        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid")
        if hum then hum.UseJumpPower = state hum.JumpPower = state and customJumpPower or 50 end
    end
})
PlayerTab:Input({
    Title = "Jump Power Value", Default = "50", Placeholder = "Contoh: 50",
    Callback = function(val) local num = tonumber(val) if num then customJumpPower = num end end
})

PlayerTab:Section({ Title = "2. Gerakan & Utilitas" })
PlayerTab:Toggle({ Title = "Bunny Hop", Default = false, Callback = function(state) bunnyHopEnabled = state end })
PlayerTab:Toggle({ 
    Title = "Noclip", Default = false, 
    Callback = function(state) 
        noclipEnabled = state 
        if not state then
            local char = LocalPlayer.Character
            if char then
                for _, part in pairs(char:GetDescendants()) do
                    if part:IsA("BasePart") then part.CanCollide = true end
                end
            end
        end
    end 
})

UserInputService.JumpRequest:Connect(function()
    if infJumpEnabled then
        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)
PlayerTab:Toggle({ Title = "Infinite Jump", Default = false, Callback = function(state) infJumpEnabled = state end })
PlayerTab:Toggle({Title = "Shift Lock", Default = false, Callback = function(V) _G.ShiftLockEnabled = V end})

PlayerTab:Section({ Title = "3. Proteksi & Ekstra" })
PlayerTab:Button({
    Title = "Reset Character",
    Callback = function()
        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.Health = 0 end
    end
})
PlayerTab:Toggle({ Title = "Anti AFK", Default = false, Callback = function(state) antiAfkEnabled = state end })


-- ==================== TAB 4: VISUAL / ESP & SPECTATE ====================
local VisualTab = Window:Tab({ Title = "Visual / ESP", Icon = "eye" })
local rainbowHitboxEnabled = false
local hitboxSizeValue = 5 
local rainbowEspEnabled = false
local rainbowHealthEspEnabled = false
local rainbowChamsEspEnabled = false
local fullBrightEnabled = false
local brightLevelValue = 2 
local fovEnabled = false
local fovValue = 70

local spectateEnabled = false
local selectedSpectateTarget = nil

VisualTab:Section({ Title = "1. Spectate Player" })
local function getPlayerNames()
    local list = {}
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then table.insert(list, p.Name) end
    end
    if #list == 0 then table.insert(list, "Tidak ada player") end
    return list
end

local visualDropdownRef = VisualTab:Dropdown({
    Title = "Pilih Player untuk Dispectate", Values = getPlayerNames(),
    Callback = function(val) selectedSpectateTarget = val end
})
VisualTab:Button({
    Title = "Refresh List Spectate",
    Callback = function()
        local newNames = getPlayerNames()
        pcall(function() visualDropdownRef:Clear() end)
        visualDropdownRef:SetValues(newNames)
        WindUI:Notify({ Title = "Refreshed", Content = "List Spectate diperbarui!", Duration = 2 })
    end
})
VisualTab:Toggle({
    Title = "Aktifkan Spectate", Default = false,
    Callback = function(state)
        spectateEnabled = state
        if not state then
            Camera.CameraSubject = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") or LocalPlayer.Character
        end
    end
})

VisualTab:Section({ Title = "2. Rainbow HitBox & ESP Player" })
VisualTab:Toggle({
    Title = "Rainbow Hitbox ESP", Default = false,
    Callback = function(state)
        rainbowHitboxEnabled = state
        if not state then
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                    local hrp = p.Character.HumanoidRootPart
                    hrp.Size = Vector3.new(2, 2, 1)
                    hrp.Transparency = 1
                    hrp.CanCollide = true
                end
            end
        end
    end
})
VisualTab:Input({
    Title = "Size HitBox", Default = "5", Placeholder = "Masukkan angka...",
    Callback = function(value) local num = tonumber(value) if num then hitboxSizeValue = num end end
})
VisualTab:Toggle({Title = "Rainbow Esp", Default = false, Callback = function(state) rainbowEspEnabled = state end})
VisualTab:Toggle({Title = "Rainbow Health esp", Default = false, Callback = function(state) rainbowHealthEspEnabled = state end})
VisualTab:Toggle({Title = "Rainbow Chamts esp", Default = false, Callback = function(state) rainbowChamsEspEnabled = state end})

VisualTab:Section({ Title = "3. Brightness and POV" })
VisualTab:Toggle({
    Title = "Full Bright", Default = false,
    Callback = function(state)
        fullBrightEnabled = state
        if not state then Lighting.Brightness = 1 Lighting.GlobalShadows = true end
    end
})
VisualTab:Input({
    Title = "Atur nilai bright", Default = "2", Placeholder = "Contoh: 2, 5",
    Callback = function(val) local num = tonumber(val) if num then brightLevelValue = num end end
})
VisualTab:Toggle({
    Title = "Fov", Default = false,
    Callback = function(state) fovEnabled = state if not state then Camera.FieldOfView = 70 end end
})
VisualTab:Input({
    Title = "Nilai Fov", Default = "70", Placeholder = "Contoh: 90, 120",
    Callback = function(val) local num = tonumber(val) if num then fovValue = num end end
})


-- ==================== TAB 5: WORLD ====================
local WorldTab = Window:Tab({ Title = "World", Icon = "globe" })
WorldTab:Section({ Title = "1. Performa Game" })
WorldTab:Button({
    Title = "FPS Boost",
    Callback = function()
        pcall(function()
            for _, v in pairs(Lighting:GetChildren()) do
                if v:IsA("PostEffect") or v:IsA("Sky") or v:IsA("Atmosphere") then v.Enabled = false end
            end
            Lighting.GlobalShadows = false
            WindUI:Notify({ Title = "FPS Boost", Content = "Grafik dioptimalkan!", Duration = 3 })
        end)
    end
})
WorldTab:Section({ Title = "2. Navigasi Server" })
WorldTab:Button({
    Title = "Server Hop",
    Callback = function()
        pcall(function()
            local servers = {}
            local req = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Asc&limit=100"))
            if req and req.data then
                for _, s in pairs(req.data) do
                    if s.playing < s.maxPlayers and s.id ~= game.JobId then table.insert(servers, s.id) end
                end
            end
            if #servers > 0 then TeleportService:TeleportToPlaceInstance(game.PlaceId, servers[math.random(1, #servers)], LocalPlayer) end
        end)
    end
})
WorldTab:Button({ Title = "Rejoin Server", Callback = function() TeleportService:Teleport(game.PlaceId, LocalPlayer) end })
WorldTab:Button({ Title = "Copy JobId", Callback = function() pcall(function() setclipboard(game.JobId) end) end })


-- ==================== TAB 6: TROLL, TP & CUSTOM SLOTS ====================
local TrollTab = Window:Tab({ Title = "Troll & TP", Icon = "crosshair" })
local selectedTarget = nil
local globalRunSpeed = 16
local ghostOtherEnabled = false
local flingAllEnabled = false

TrollTab:Section({ Title = "1. Fitur Troll" })
TrollTab:Toggle({
    Title = "Ghost Other Player", Default = false,
    Callback = function(state)
        ghostOtherEnabled = state
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                for _, part in pairs(p.Character:GetDescendants()) do
                    if part:IsA("BasePart") then part.Transparency = state and 1 or 0 end
                end
            end
        end
    end
})
TrollTab:Toggle({ Title = "Fling All (Dekat Aktif)", Default = false, Callback = function(state) flingAllEnabled = state end })
TrollTab:Button({
    Title = "Bring All Players",
    Callback = function()
        local myHrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if myHrp then
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                    p.Character.HumanoidRootPart.CFrame = myHrp.CFrame + Vector3.new(3, 0, 0)
                end
            end
        end
    end
})

TrollTab:Section({ Title = "2. Teleport ke Player" })
local trollDropdownRef = TrollTab:Dropdown({
    Title = "List Player", Values = getPlayerNames(),
    Callback = function(val) selectedTarget = val end
})
TrollTab:Button({
    Title = "Teleport ke Player Terpilih",
    Callback = function()
        if selectedTarget and selectedTarget ~= "Tidak ada player" then
            local targetPlayer = Players:FindFirstChild(selectedTarget)
            if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
                local myHrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                if myHrp then 
                    myHrp.CFrame = targetPlayer.Character.HumanoidRootPart.CFrame + Vector3.new(0, 3, 0) 
                    WindUI:Notify({ Title = "Berhasil", Content = "Teleport ke " .. selectedTarget, Duration = 2 })
                end
            else
                WindUI:Notify({ Title = "Gagal", Content = "Player target tidak valid!", Duration = 2 })
            end
        else
            WindUI:Notify({ Title = "Gagal", Content = "Pilih player yang benar!", Duration = 2 })
        end
    end
})
TrollTab:Button({
    Title = "Refresh Player List",
    Callback = function()
        local newNames = getPlayerNames()
        pcall(function() trollDropdownRef:Clear() end)
        trollDropdownRef:SetValues(newNames)
        WindUI:Notify({ Title = "Refreshed", Content = "List Player berhasil diperbarui!", Duration = 2 })
    end
})


-- ==================== 5 INSTANT TELEPORT ====================
TrollTab:Section({ Title = "3. Instant Teleport" })
local savedLocsTP = {}

for i = 1, 5 do
    TrollTab:Button({
        Title = "Save Posisi",
        Callback = function()
            local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                savedLocsTP[i] = hrp.CFrame
                WindUI:Notify({ Title = "Berhasil", Content = "Posisi Instant TP disimpan!", Duration = 2 })
            end
        end
    })
    TrollTab:Button({
        Title = "Teleport Instan",
        Callback = function()
            local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if hrp and savedLocsTP[i] then
                hrp.CFrame = savedLocsTP[i]
                WindUI:Notify({ Title = "Berhasil", Content = "Teleport Instan!", Duration = 2 })
            else
                WindUI:Notify({ Title = "Gagal", Content = "Posisi belum disimpan!", Duration = 2 })
            end
        end
    })
end


-- ==================== 5 RUN ====================
TrollTab:Section({ Title = "4. Run" })
local savedLocsRun = {}
TrollTab:Input({
    Title = "Atur Global Run Speed", Default = "16", Placeholder = "Kecepatan jalan...",
    Callback = function(val) local num = tonumber(val) if num then globalRunSpeed = math.clamp(num, 1, 100) end end
})

for i = 1, 5 do
    TrollTab:Button({
        Title = "Save Posisi",
        Callback = function()
            local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                savedLocsRun[i] = hrp.CFrame
                WindUI:Notify({ Title = "Berhasil", Content = "Posisi Run disimpan!", Duration = 2 })
            end
        end
    })
    TrollTab:Button({
        Title = "Run",
        Callback = function()
            local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid")
            if hum and savedLocsRun[i] then
                hum.WalkSpeed = globalRunSpeed
                hum:MoveTo(savedLocsRun[i].Position)
                WindUI:Notify({ Title = "Berhasil", Content = "Run!", Duration = 2 })
            else
                WindUI:Notify({ Title = "Gagal", Content = "Posisi belum disimpan!", Duration = 2 })
            end
        end
    })
end


-- ==================== TAB 7: SETTINGS (UI & FONT CUSTOMIZATION) ====================
local SettingsTab = Window:Tab({ Title = "Settings", Icon = "settings" })

SettingsTab:Section({ Title = "1. Kustomisasi Tema & Warna UI" })
SettingsTab:Dropdown({
    Title = "Pilih Tema Warna UI",
    Values = { "Dark", "Light", "Amethyst", "Rose", "Emerald" },
    Default = "Dark",
    Callback = function(themeName)
        pcall(function()
            Window:SetTheme(themeName)
            WindUI:Notify({ Title = "Tema Diubah", Content = "Tema UI diganti ke: " .. themeName, Duration = 2 })
        end)
    end
})

SettingsTab:Section({ Title = "2. Pengaturan Font" })
SettingsTab:Dropdown({
    Title = "Pilih Jenis Font",
    Values = { "Default", "Code", "Gotham", "SourceSans" },
    Default = "Default",
    Callback = function(fontName)
        pcall(function()
            WindUI:Notify({ Title = "Font Diubah", Content = "Font diganti ke: " .. fontName, Duration = 2 })
        end)
    end
})


-- ==================== LOOP RUNTIME GLOBAL ====================
RunService.RenderStepped:Connect(function()
    pcall(function()
        -- Logika Spectate
        if spectateEnabled and selectedSpectateTarget and selectedSpectateTarget ~= "Tidak ada player" then
            local targetPlayer = Players:FindFirstChild(selectedSpectateTarget)
            if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChildOfClass("Humanoid") then
                Camera.CameraSubject = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
            end
        end

        local tickTime = tick() * 5
        local rainbowColor = Color3.fromHSV(tickTime % 1, 1, 1)

        -- Loop ESP HANYA untuk Player Asli (Dilengkapi Nama & Health)
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                local char = p.Character
                local hrp = char:FindFirstChild("HumanoidRootPart")
                
                -- 1. Rainbow Hitbox ESP
                if rainbowHitboxEnabled and hrp then
                    hrp.Size = Vector3.new(hitboxSizeValue, hitboxSizeValue, hitboxSizeValue)
                    hrp.Transparency = 0.5
                    hrp.Color = rainbowColor
                    hrp.CanCollide = false
                end

                -- 2. Rainbow ESP & Chams
                local highlight = char:FindFirstChild("KyZenHighlight")
                if (rainbowEspEnabled or rainbowChamsEspEnabled) then
                    if not highlight then
                        highlight = Instance.new("Highlight")
                        highlight.Name = "KyZenHighlight"
                        highlight.Parent = char
                    end
                    highlight.Enabled = true
                    highlight.FillColor = rainbowColor
                    highlight.OutlineColor = rainbowColor
                    highlight.FillTransparency = rainbowChamsEspEnabled and 0.5 or 1
                    highlight.OutlineTransparency = 0
                else
                    if highlight then highlight:Destroy() end
                end

                -- 3. Rainbow Health & Name ESP (BillboardGui di Kepala)
                local head = char:FindFirstChild("Head")
                local hum = char:FindFirstChild("Humanoid")
                local billboard = head and head:FindFirstChild("KyZenNameHealthESP")
                if rainbowHealthEspEnabled and head and hum then
                    if not billboard then
                        billboard = Instance.new("BillboardGui")
                        billboard.Name = "KyZenNameHealthESP"
                        billboard.Size = UDim2.new(0, 120, 0, 50)
                        billboard.StudsOffset = Vector3.new(0, 3, 0)
                        billboard.AlwaysOnTop = true
                        billboard.Parent = head

                        local nameLabel = Instance.new("TextLabel")
                        nameLabel.Name = "NameText"
                        nameLabel.Size = UDim2.new(1, 0, 0.5, 0)
                        nameLabel.BackgroundTransparency = 1
                        nameLabel.TextScaled = true
                        nameLabel.Font = Enum.Font.SourceSansBold
                        nameLabel.Text = p.Name
                        nameLabel.Parent = billboard

                        local healthLabel = Instance.new("TextLabel")
                        healthLabel.Name = "HealthText"
                        healthLabel.Size = UDim2.new(1, 0, 0.5, 0)
                        healthLabel.Position = UDim2.new(0, 0, 0.5, 0)
                        healthLabel.BackgroundTransparency = 1
                        healthLabel.TextScaled = true
                        healthLabel.Font = Enum.Font.SourceSansBold
                        healthLabel.Parent = billboard
                    end
                    local healthLabel = billboard:FindFirstChild("HealthText")
                    local nameLabel = billboard:FindFirstChild("NameText")
                    if healthLabel and nameLabel then
                        healthLabel.Text = "HP: " .. math.floor(hum.Health) .. "/" .. math.floor(hum.MaxHealth)
                        healthLabel.TextColor3 = rainbowColor
                        nameLabel.TextColor3 = rainbowColor
                    end
                else
                    if billboard then billboard:Destroy() end
                end
            end
        end

        -- Loop Kontrol Player & Environment Lokal
        local char = LocalPlayer.Character
        if not char then return end
        local hum = char:FindFirstChild("Humanoid")
        local hrp = char:FindFirstChild("HumanoidRootPart")
        
        if flyToggled and hrp and bodyVelocity and bodyGyro then
            bodyGyro.CFrame = Camera.CFrame
            local moveDirection = hum.MoveDirection
            local ySpeed = 0
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then ySpeed = flySpeed end
            bodyVelocity.Velocity = (Camera.CFrame.LookVector * (moveDirection.Magnitude > 0 and flySpeed or 0)) + Vector3.new(0, ySpeed, 0)
        end
        if walkSpeedEnabled and hum then hum.WalkSpeed = customWalkSpeed end
        if jumpPowerEnabled and hum then hum.JumpPower = customJumpPower end
        if bunnyHopEnabled and hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
        if noclipEnabled then
            for _, part in pairs(char:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
        if fullBrightEnabled then Lighting.Brightness = brightLevelValue Lighting.GlobalShadows = false end
        if fovEnabled then Camera.FieldOfView = fovValue end
    end)
end)

WindUI:Notify({ Title = "KyZen HUB", Content = "Settings Tab Added Successfully! ✅", Duration = 3 })
