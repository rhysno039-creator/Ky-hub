-- ================= KEY SYSTEM (Airflow Style) =================
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local keysList = {"From", "Get", "KyZen", "Owner", "Ganteng", "Follow", "Saluran", "Tiktok", "Om", "Tante"}

local seed = tick() * os.time()
math.randomseed(seed)
math.random(); math.random(); math.random()
local sessionKey = keysList[math.random(1, #keysList)]

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "KyZenAirflowKeySys"
ScreenGui.Parent = CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 480, 0, 270)
MainFrame.Position = UDim2.new(0.5, -240, 0.5, -135)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 14)
UICorner.Parent = MainFrame

local UIStroke = Instance.new("UIStroke")
UIStroke.Color = Color3.fromRGB(70, 90, 220)
UIStroke.Transparency = 0.3
UIStroke.Thickness = 1.5
UIStroke.Parent = MainFrame

-- Header Title & Subtitle (Airflow Style)
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(0, 200, 0, 25)
Title.Position = UDim2.new(0, 20, 0, 18)
Title.BackgroundTransparency = 1
Title.Text = "KyZen HUB"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = MainFrame

local Subtitle = Instance.new("TextLabel")
Subtitle.Size = UDim2.new(0, 200, 0, 15)
Subtitle.Position = UDim2.new(0, 20, 0, 40)
Subtitle.BackgroundTransparency = 1
Subtitle.Text = "Key system"
Subtitle.TextColor3 = Color3.fromRGB(130, 130, 150)
Subtitle.TextSize = 12
Subtitle.Font = Enum.Font.Gotham
Subtitle.TextXAlignment = Enum.TextXAlignment.Left
Subtitle.Parent = MainFrame

-- Close Button (Triggers Pop-Up)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 32, 0, 32)
CloseBtn.Position = UDim2.new(1, -42, 0, 15)
CloseBtn.BackgroundColor3 = Color3.fromRGB(230, 50, 50)
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Text = "×"
CloseBtn.TextSize = 22
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = MainFrame

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 8)
CloseCorner.Parent = CloseBtn

-- Input Key Box ("Place key here")
local KeyInput = Instance.new("TextBox")
KeyInput.Size = UDim2.new(0.55, 0, 0, 45)
KeyInput.Position = UDim2.new(0.04, 0, 0, 75)
KeyInput.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
KeyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyInput.PlaceholderText = "Place key here"
KeyInput.PlaceholderColor3 = Color3.fromRGB(100, 100, 120)
KeyInput.TextSize = 13
KeyInput.Font = Enum.Font.GothamMedium
KeyInput.ClearTextOnFocus = false
KeyInput.Parent = MainFrame

local InputCorner = Instance.new("UICorner")
InputCorner.CornerRadius = UDim.new(0, 8)
InputCorner.Parent = KeyInput

local InputStroke = Instance.new("UIStroke")
InputStroke.Color = Color3.fromRGB(45, 45, 65)
InputStroke.Thickness = 1
InputStroke.Parent = KeyInput

-- Get Key Button
local GetKeyBtn = Instance.new("TextButton")
GetKeyBtn.Size = UDim2.new(0.33, 0, 0, 45)
GetKeyBtn.Position = UDim2.new(0.62, 0, 0, 75)
GetKeyBtn.BackgroundColor3 = Color3.fromRGB(45, 65, 200)
GetKeyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
GetKeyBtn.Text = "Get a key"
GetKeyBtn.TextSize = 13
GetKeyBtn.Font = Enum.Font.GothamBold
GetKeyBtn.Parent = MainFrame

local GetKeyCorner = Instance.new("UICorner")
GetKeyCorner.CornerRadius = UDim.new(0, 8)
GetKeyCorner.Parent = GetKeyBtn

-- Bottom Cards (Airflow Style Info Cards)[span_2](start_span)[span_2](end_span)
local Card1 = Instance.new("Frame")
Card1.Size = UDim2.new(0.3, 0, 0, 95)
Card1.Position = UDim2.new(0.04, 0, 0, 140)
Card1.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
Card1.BorderSizePixel = 0
Card1.Parent = MainFrame

local Card1Corner = Instance.new("UICorner")
Card1Corner.CornerRadius = UDim.new(0, 8)
Card1Corner.Parent = Card1

local C1Title = Instance.new("TextLabel")
C1Title.Size = UDim2.new(1, -10, 0, 20)
C1Title.Position = UDim2.new(0, 10, 0, 10)
C1Title.BackgroundTransparency = 1
C1Title.Text = "Status"
C1Title.TextColor3 = Color3.fromRGB(120, 120, 140)
C1Title.TextSize = 11
C1Title.Font = Enum.Font.GothamBold
C1Title.TextXAlignment = Enum.TextXAlignment.Left
C1Title.Parent = Card1

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, -10, 0, 40)
StatusLabel.Position = UDim2.new(0, 10, 0, 35)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "Waiting for key..."
StatusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
StatusLabel.TextSize = 12
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.TextXAlignment = Enum.TextXAlignment.Left
StatusLabel.TextWrapped = true
StatusLabel.Parent = Card1

-- Card 2: Supported
local Card2 = Instance.new("Frame")
Card2.Size = UDim2.new(0.3, 0, 0, 95)
Card2.Position = UDim2.new(0.35, 0, 0, 140)
Card2.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
Card2.BorderSizePixel = 0
Card2.Parent = MainFrame

local Card2Corner = Instance.new("UICorner")
Card2Corner.CornerRadius = UDim.new(0, 8)
Card2Corner.Parent = Card2

local C2Title = Instance.new("TextLabel")
C2Title.Size = UDim2.new(1, -10, 0, 20)
C2Title.Position = UDim2.new(0, 10, 0, 10)
C2Title.BackgroundTransparency = 1
C2Title.Text = "Supported"
C2Title.TextColor3 = Color3.fromRGB(120, 120, 140)
C2Title.TextSize = 11
C2Title.Font = Enum.Font.GothamBold
C2Title.TextXAlignment = Enum.TextXAlignment.Left
C2Title.Parent = Card2

local C2Desc = Instance.new("TextLabel")
C2Desc.Size = UDim2.new(1, -10, 0, 40)
C2Desc.Position = UDim2.new(0, 10, 0, 35)
C2Desc.BackgroundTransparency = 1
C2Desc.Text = "Universal Game"
C2Desc.TextColor3 = Color3.fromRGB(0, 255, 120)
C2Desc.TextSize = 12
C2Desc.Font = Enum.Font.GothamBold
C2Desc.TextXAlignment = Enum.TextXAlignment.Left
C2Desc.Parent = Card2

-- Card 3: Creator
local Card3 = Instance.new("Frame")
Card3.Size = UDim2.new(0.3, 0, 0, 95)
Card3.Position = UDim2.new(0.66, 0, 0, 140)
Card3.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
Card3.BorderSizePixel = 0
Card3.Parent = MainFrame

local Card3Corner = Instance.new("UICorner")
Card3Corner.CornerRadius = UDim.new(0, 8)
Card3Corner.Parent = Card3

local C3Title = Instance.new("TextLabel")
C3Title.Size = UDim2.new(1, -10, 0, 20)
C3Title.Position = UDim2.new(0, 10, 0, 10)
C3Title.BackgroundTransparency = 1
C3Title.Text = "Creator"
C3Title.TextColor3 = Color3.fromRGB(120, 120, 140)
C3Title.TextSize = 11
C3Title.Font = Enum.Font.GothamBold
C3Title.TextXAlignment = Enum.TextXAlignment.Left
C3Title.Parent = Card3

local C3Desc = Instance.new("TextLabel")
C3Desc.Size = UDim2.new(1, -10, 0, 40)
C3Desc.Position = UDim2.new(0, 10, 0, 35)
C3Desc.BackgroundTransparency = 1
C3Desc.Text = "KyZen HUB"
C3Desc.TextColor3 = Color3.fromRGB(100, 150, 255)
C3Desc.TextSize = 12
C3Desc.Font = Enum.Font.GothamBold
C3Desc.TextXAlignment = Enum.TextXAlignment.Left
C3Desc.Parent = Card3

-- ================= POP-UP CONFIRMATION FRAME =================
local PopupFrame = Instance.new("Frame")
PopupFrame.Size = UDim2.new(0, 260, 0, 140)
PopupFrame.Position = UDim2.new(0.5, -130, 0.5, -70)
PopupFrame.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
PopupFrame.BorderSizePixel = 0
PopupFrame.Visible = false
PopupFrame.ZIndex = 10
PopupFrame.Parent = ScreenGui

local PopupCorner = Instance.new("UICorner")
PopupCorner.CornerRadius = UDim.new(0, 10)
PopupCorner.Parent = PopupFrame

local PopupStroke = Instance.new("UIStroke")
PopupStroke.Color = Color3.fromRGB(70, 70, 90)
PopupStroke.Thickness = 1
PopupStroke.Parent = PopupFrame

local PopupTitle = Instance.new("TextLabel")
PopupTitle.Size = UDim2.new(1, 0, 0, 35)
PopupTitle.Position = UDim2.new(0, 0, 0, 15)
PopupTitle.BackgroundTransparency = 1
PopupTitle.Text = "Close Menu?"
PopupTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
PopupTitle.TextSize = 15
PopupTitle.Font = Enum.Font.GothamBold
PopupTitle.ZIndex = 11
PopupTitle.Parent = PopupFrame

local PopupDesc = Instance.new("TextLabel")
PopupDesc.Size = UDim2.new(1, 0, 0, 20)
PopupDesc.Position = UDim2.new(0, 0, 0, 45)
PopupDesc.BackgroundTransparency = 1
PopupDesc.Text = "Are you sure you want to exit?"
PopupDesc.TextColor3 = Color3.fromRGB(150, 150, 170)
PopupDesc.TextSize = 12
PopupDesc.Font = Enum.Font.Gotham
PopupDesc.ZIndex = 11
PopupDesc.Parent = PopupFrame

-- Tombol No
local NoBtn = Instance.new("TextButton")
NoBtn.Size = UDim2.new(0, 105, 0, 35)
NoBtn.Position = UDim2.new(0, 15, 0, 85)
NoBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 60)
NoBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
NoBtn.Text = "No"
NoBtn.TextSize = 13
NoBtn.Font = Enum.Font.GothamBold
NoBtn.ZIndex = 11
NoBtn.Parent = PopupFrame

local NoCorner = Instance.new("UICorner")
NoCorner.CornerRadius = UDim.new(0, 6)
NoCorner.Parent = NoBtn

-- Tombol Yes
local YesBtn = Instance.new("TextButton")
YesBtn.Size = UDim2.new(0, 105, 0, 35)
YesBtn.Position = UDim2.new(1, -120, 0, 85)
YesBtn.BackgroundColor3 = Color3.fromRGB(220, 50, 50)
YesBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
YesBtn.Text = "Yes"
YesBtn.TextSize = 13
YesBtn.Font = Enum.Font.GothamBold
YesBtn.ZIndex = 11
YesBtn.Parent = PopupFrame

local YesCorner = Instance.new("UICorner")
YesCorner.CornerRadius = UDim.new(0, 6)
YesCorner.Parent = YesBtn

-- Logika Pop-up
CloseBtn.MouseButton1Click:Connect(function()
    PopupFrame.Visible = true
end)

NoBtn.MouseButton1Click:Connect(function()
    PopupFrame.Visible = false
end)

YesBtn.MouseButton1Click:Connect(function()
    LocalPlayer:Kick("KyZen HUB: Verification closed.")
end)

local keyAuthenticated = false

GetKeyBtn.MouseButton1Click:Connect(function()
    pcall(function() setclipboard(sessionKey) end)
    KeyInput.Text = sessionKey
    StatusLabel.Text = "Key copied & auto-filled!"
    StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 120)
end)

KeyInput:GetPropertyChangedSignal("Text"):Connect(function()
    if keyAuthenticated then return end
    local input = KeyInput.Text
    local isValid = false
    
    for _, k in pairs(keysList) do
        if input == k or input == sessionKey then
            isValid = true
            break
        end
    end
    
    if isValid then
        keyAuthenticated = true
        StatusLabel.Text = "Verified! Opening..."
        StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
        task.wait(0.5)
        ScreenGui:Destroy()
    end
end)

while not keyAuthenticated do
    task.wait(0.1)
end

-- ================= MAIN SCRIPT HUB =================
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "KyZen HUB | IS BACK",
    Icon = "rbxassetid://10723415303",
    Author = "KyZen",
    Folder = "KyZenHubConfig",
    Theme = "Dark",
    Transparent = true,
    Resizable = true,
})

-- 13 Menu Lengkap (Default ke Home + Menu Teleport)
local Tabs = {
    Home = Window:Tab({ Title = "Home", Icon = "home" }),
    Player = Window:Tab({ Title = "Player", Icon = "user" }),
    Movement = Window:Tab({ Title = "Movement", Icon = "footprints" }),
    ESP = Window:Tab({ Title = "ESP", Icon = "users" }),
    Visual = Window:Tab({ Title = "Visual", Icon = "eye" }),
    CFrame = Window:Tab({ Title = "CFrame", Icon = "zap" }),
    Fly = Window:Tab({ Title = "Fly", Icon = "navigation" }),
    Combat = Window:Tab({ Title = "Combat", Icon = "sword" }),
    Troll = Window:Tab({ Title = "Troll", Icon = "smile" }),
    World = Window:Tab({ Title = "World", Icon = "globe" }),
    Teleport = Window:Tab({ Title = "Teleport", Icon = "map-pin" }),
    Utility = Window:Tab({ Title = "Utility", Icon = "tool" }),
    Settings = Window:Tab({ Title = "Settings", Icon = "settings" }),
}

-- Kosongkan Section di Setiap Menu
Tabs.Home:Section({ Title = "Home Menu" })
Tabs.Player:Section({ Title = "Player Menu" })
Tabs.Movement:Section({ Title = "Movement Menu" })
Tabs.ESP:Section({ Title = "ESP Menu" })
Tabs.Visual:Section({ Title = "Visual Menu" })
Tabs.CFrame:Section({ Title = "CFrame Menu" })
Tabs.Fly:Section({ Title = "Fly Menu" })
Tabs.Combat:Section({ Title = "Combat Menu" })
Tabs.Troll:Section({ Title = "Troll Menu" })
Tabs.World:Section({ Title = "World Menu" })
Tabs.Teleport:Section({ Title = "Teleport Menu" })
Tabs.Utility:Section({ Title = "Utility Menu" })
Tabs.Settings:Section({ Title = "Settings Menu" })

WindUI:Notify({
    Title = "KyZen HUB | IS BACK",
    Content = "Welcome! Key verified automatically.",
    Duration = 5
})
